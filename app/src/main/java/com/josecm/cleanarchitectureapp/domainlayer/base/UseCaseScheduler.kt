package com.josecm.cleanarchitectureapp.domainlayer.base

/**
 * Interface for schedulers.
 */
interface UseCaseScheduler {
  /*  fun <Q : UseCase.RequestValues, R : UseCase.ResponseValue> execute(
        useCase: UseCase<Q, R>,
        requestValues: Q,
        callback: UseCase.UseCaseCallback<R>
    )*/

    //TODO quitar esta clase hasta que se diga lo contrario
}